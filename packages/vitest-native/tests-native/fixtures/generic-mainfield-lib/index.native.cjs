// What Metro picks. Plenty of packages that are not React Native libraries at all
// still ship this field — algolia's clients, nanoid, msgpackr — usually pointing at
// a browser or ESM build.
module.exports = { entry: "react-native-field" };
